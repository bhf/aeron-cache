## Aeron Cache NextJS UI

A UI for Aeron Cache built on NextJS with ShadCN and Tailwind for components.

```bash
docker build . -t aeron-cache-ui -f Dockerfile 
docker run -p 3000:3000 -d aeron-cache-ui
```