apiVersion: v1
kind: Service
metadata:
  name: aeroncache-ui-nextjs
spec:
  type: LoadBalancer
  externalTrafficPolicy: Local
  selector:
    tier: frontend
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000