apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: aeroncache-ws
  namespace: default
  labels:
    app.kubernetes.io/name: aeroncache-ws
    app.kubernetes.io/instance: aeroncache-ws
spec:
  replicas: 1
  podManagementPolicy: Parallel
  serviceName: aeroncache-ws
  selector:
    matchLabels:
      app.kubernetes.io/name: aeroncache-ws
      app.kubernetes.io/instance: aeroncache-ws
  template:
    metadata:
      annotations:
        {}
      labels:
        app.kubernetes.io/name: aeroncache-ws
        app.kubernetes.io/instance: aeroncache-ws
    spec:
      serviceAccountName: aeroncache-ws
      volumes:
        - emptyDir:
            medium: Memory
            sizeLimit: 1Gi
          name: shm
      containers:
        - name: aeroncache-ws
          image: docker.io/library/aeroncache-ws-javalin
          imagePullPolicy: Never
          ports:
            - containerPort: 7071
              protocol: TCP
          volumeMounts:
            - mountPath: /dev/shm
              name: shm
          env:
            - name: POD_ADDRESS
              value: 127.0.0.1
          envFrom:
            - configMapRef:
                name: aeroncache-ws-config