apiVersion: v1
kind: Service
metadata:
  name: {{ include "aeronCacheChart.fullname" . }}
  labels:
    {{- include "aeronCacheChart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "aeronCacheChart.selectorLabels" . | nindent 4 }}
