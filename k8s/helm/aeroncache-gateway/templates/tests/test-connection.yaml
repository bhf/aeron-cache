apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "aeroncache-gateway.fullname" . }}-test-connection"
  labels:
    {{- include "aeroncache-gateway.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "aeroncache-gateway.fullname" . }}:{{ .Values.service.port }}/liveness/']
  restartPolicy: Never
