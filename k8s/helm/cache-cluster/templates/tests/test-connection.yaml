apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "aeronCacheChart.fullname" . }}-test-connection"
  labels:
    {{- include "aeronCacheChart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "aeronCacheChart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
