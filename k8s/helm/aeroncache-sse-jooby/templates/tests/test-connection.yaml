apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "aeroncache-sse-jooby.fullname" . }}-test-connection"
  labels:
    {{- include "aeroncache-sse-jooby.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "aeroncache-sse-jooby.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
