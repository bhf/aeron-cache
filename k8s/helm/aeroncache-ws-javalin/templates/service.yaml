apiVersion: v1
kind: Service
metadata:
  name: {{ include "aeroncache-ws-javalin.fullname" . }}
  labels:
    {{- include "aeroncache-ws-javalin.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "aeroncache-ws-javalin.selectorLabels" . | nindent 4 }}
