apiVersion: v1
kind: ConfigMap
metadata:
  name: aeroncache-ws-javalin-config
  namespace: {{ .Release.namespace }}
data:
  DNS_DELAY: "true"
  CACHE_MODE: {{ tpl .Values.cacheMode . | quote }}
  CLUSTER_ADDRESSES: {{ tpl .Values.clusterAddresses . | quote }}
  REQUEST_PUB_HOST: {{ tpl .Values.requestPubHost . | quote }}
  {{ printf "environments/configmap.%s.yaml" .Values.env | .Files.Get | indent 2 }}
